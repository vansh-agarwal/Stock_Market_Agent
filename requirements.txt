# Finance Agent dependencies
# Pinned to major.minor of currently installed versions (pip freeze, Sep 2026).
# Use ~= (compatible release) so patch updates are allowed but minor/major bumps are not.
# Re-run `pip freeze` and update these after any intentional upgrade.

# LLM inference
groq~=1.7

# Stock data
yfinance~=1.7

# Web framework
Flask~=3.0
Flask-Cors~=4.0

# HTTP client
requests~=2.34

# Environment variables
python-dotenv~=1.0

# RAG pipeline
# ChromaDB 1.5.x uses a Rust-backed storage engine that strictly rejects
# duplicate IDs within a single upsert batch.
# Do not upgrade past 1.x without testing upsert idempotency.
chromadb~=1.5

# Sentence embeddings — model: all-MiniLM-L6-v2 (384-dim)
sentence-transformers~=6.0

# HTML parsing (RBI/BSE scrapers)
beautifulsoup4~=4.12
lxml~=5.2

# File upload parsing dependencies
pdfplumber>=0.10.0
python-docx>=1.0.0
openpyxl>=3.1.0
pandas>=2.0.0